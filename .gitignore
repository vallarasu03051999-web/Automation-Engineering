node_modules/
dist/
/playwright/.cache/

# Raw per-run artifacts (traces, duplicate videos, screenshots) — the committed
# deliverable is /playwright-report/, which already embeds the report + video.
/test-results/

# Local environment overrides — real per-developer/per-environment values.
# .env.example / .env.staging.example are committed as templates.
.env
.env.*
!.env.example
!.env.*.example

# Session storage state generated by tests/global-setup.ts on each run.
/playwright/.auth/*.json
